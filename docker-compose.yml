version: '3.8'
services:
  postgres:
    container_name: cloud_box
    image: 'postgres:17'
    environment:
      POSTGRES_DB: cloud_box
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
    ports:
      - '5432:5432'